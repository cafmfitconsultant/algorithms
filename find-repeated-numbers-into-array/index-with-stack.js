// must be 1 <= and >= n
const numbers = [6, 4, 3, 5, 2, 5, 7, 2].sort();
const stack = [];
console.log(numbers);
for (let i = 0; i < numbers.length - 1; i++) {
    const current = numbers[i];
    if (current == numbers.at(-1)) {
    }
}

console.log(stack);